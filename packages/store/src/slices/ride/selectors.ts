/**
 * Defines selectors for important user information.
 * @TODO
 * @author Elias Schablowski
 * @format
 */

import { RootState } from "../../store";

export const getDestination = (state: RootState) => state.destination;
